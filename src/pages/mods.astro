---
import Layout from '../layouts/Layout.astro';
import Title from '../components/Title.astro';
import mods from '../database.json';
import ModList from '../components/ModsList.jsx'; // adjust path
---

<Layout title="DREDGE Mods List">
	<main>
        <script type="module">
            const input = document.getElementById('searchInput');
            input.addEventListener('input', () => {
              console.log(input.value);
            });
        </script>

        <Title title="Available Mods" />
        <p>There are <strong>{mods.length}</strong> mods available.</p>

        <ModList mods={mods} client:load />

	</main>
</Layout>


